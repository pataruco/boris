const MPindex = require ('../src/mpindex.js');
const expect = require('chai').expect;
const assert = require('chai').assert;

describe( 'MPindex', ( ) => {
    describe('class',  ( ) => {
        it('should return an array', () => {
            let index = new MPindex ();
            assert.isArray(menu, 'what kind of tea do we want?');
        })
    })
});
